import 'package:flutter/material.dart';

class FoodDiaryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Food Diary')),
      body: Center(child: Text('Food Diary Screen')),
    );
  }
}
